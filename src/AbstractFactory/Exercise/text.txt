# 8 - 1
Tray.javaを継承したサブクラスで操作できないため、Tray.javaに操作用のメソッドが必要になる。メソッドを通してしか要素を操作できないため、データが、スーパークラスでは想定されていない形でサブクラスに操作される可能性がなくなる。
新たに実装したい操作があった場合、スーパークラスに実装しなければならないため、影響が大きい。

-> privateフィールドの実装によらない実装になる
-> アクセスメソッドが必要

# 8 - 2
具体的な工場でcreatePageを再利用したcreateYahooPageを定義し、部品には変更を入れない

# 8 - 3
処理を明示することで、コードを読む人に意図がわかりやすくなる
-> コンストラクタは継承されないため

# 8 - 4
PageはTrayに追加できるべきではないから